<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_agends_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.od.mma.event.agenda.AgendsMainActivity"
    tools:showIn="@layout/activity_agends_main">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.3"
        android:scaleType="fitXY"
        android:src="@drawable/login_bg" />

    <include
        android:id="@+id/segment_include"
        layout="@layout/agenda_segment"/>

    <android.support.v4.view.ViewPager
        android:layout_marginTop="10dp"
        android:layout_below="@+id/segment_include"
        android:id="@+id/agends_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
